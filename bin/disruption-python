#!/bin/bash

# setup/activate
# shellcheck source=/dev/null
source "$(dirname "$0")/activate.sh"

# info
if [[ $DEBUG ]]
then
{
   env | grep -i disruption
   git -C "$DISPY_BRANCH_DIR" log -1
} 1>&2
fi

# execute
python "$@"

